#pragma once

#include <cstddef>

namespace ghalo {

inline constexpr std::size_t default_min_halo = 2;
inline constexpr std::size_t default_max_halo = 1024;
inline constexpr std::size_t default_halo_multiplier = 2;

} // namespace ghalo
